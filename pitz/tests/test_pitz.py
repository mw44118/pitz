# vim: set expandtab ts=4 sw=4 filetype=python:

import pitz

def test_thing_1():
    """
    Build a few things and a simple query to get the ones that have a
    certain value for an attribute.
    """
    
def test_tasks_and_milestones():

    """
    Make some tasks (subclasses of things) and then link a few of them
    to some milestones (also thing subclasses).
    """
