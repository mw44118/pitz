{%  for equalsign in bag.title -%}={% endfor %}
{{ bag.title }}
{%  for equalsign in bag.title -%}={% endfor %}

{{ bag.contents }}

{% for x, e in looper(entities) -%}
{% if shell_mode -%}
{{ '%4d' | format(x.index) }}: {{ getattr(e, entity_view) }}
{% else -%}
{{ getattr(e, entity_view) }}
{% endif -%}
{% endfor %}
