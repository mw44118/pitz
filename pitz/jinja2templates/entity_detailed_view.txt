{{summarized_view}}
{{line_of_dashes}}

{% for k, v in data.items() %}
{{ k }}:
{% if hasattr(data[k], 'summarized_view') -%} 
{{ data[k].summarized_view -}}
{% else -%} 
{{ data[k] -}}
{% endif %}
{% endfor %}
